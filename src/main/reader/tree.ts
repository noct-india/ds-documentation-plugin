// Moved to src/shared/tree.ts — the UI needs `folderAt` to drill into a folder
// without a round-trip to the sandbox, and the parser has no Figma dependency.
//
// This file is a leftover and can be deleted; it has been staged in
// `NOCT/AI implementation/trash/`.
export * from '../../shared/tree'
