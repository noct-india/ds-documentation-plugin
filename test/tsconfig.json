{
  // Tests run under Node, so they get Node types — the plugin source does not.
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["node", "@figma/plugin-typings"]
  },
  "include": ["./**/*.ts", "../src/**/*.ts", "../src/**/*.tsx"]
}
