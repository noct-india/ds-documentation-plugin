// Scratch file used while tuning the classifier. Superseded by:
//   test/classify.ts   — the playground, via `npm run classify`
//   test/selftest.ts   — those cases, now permanent regression tests
//
// This file and its folder can be deleted; a copy is staged in
// `NOCT/AI implementation/trash/classifier-scratch-try.ts`.
export {}
