# Checkbox

> Component set · 12 variants · in `Selectors`

## Variants and properties

| Property | Type | Options | Default |
|---|---|---|---|
| `State` | VARIANT | `Unchecked`, `Checked`, `Indeterminate` | `Unchecked` |

> ⚠️ Not documented yet — no usage rules have been written for this component set. Do not infer constraints for it; ask instead.
